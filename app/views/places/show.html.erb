<div class="container my-5">
  <div class="row">
    <div class="col-12 col-lg-8">
        <div id="carouselExampleControls" class="carousel slide" data-bs-ride="carousel">
          <div class="carousel-inner rounded-angle">
            <div class="carousel-item active">
              <%= cl_image_tag @place.photos.first.key, crop: :fill, class: "d-block w-100"%>
            </div>
            <% @place.photos[1..].each do |photo| %>
            <div class="carousel-item">
              <%= cl_image_tag photo.key, crop: :fill, class: "d-block w-100"%>
            </div>
            <% end %>
          </div>
        <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Previous</span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Next</span>
        </button>
        </div>
      </div>
    <div class="col-12 col-lg-4">
    <h2 class="mt-3 mx-2"><strong><%= @place.title %></strong></h2>
    <p class="mx-2">Proposé par :  <%= cl_image_tag @place.user.photo.key, width: 40, height: 40, crop: :fill, class: "avatar-bordered dropdown-toggle" %> <%= @place.user.authority_name %> </p>
    <p class="my-5 mx-2"><%= @place.description %> </p>
    <p class="my-5">📍<em><%= @place.address %></em></p>
    <p><i class="tag-activity m-2"><%= @place.type_of_place %></i><i class="tag-activity m-2"><%= @place.available_for %></i></p>
    <% if current_user == @place.user %>
    <p class="my-5 mx-2"><%= link_to "✏️ Modifier", edit_place_path, class: "link-solo" %></p>
    <% end %>
    <% unless current_user.nil? || current_user.local_authority %>
    <p class="my-5 mx-2"><%= link_to "💡 Proposer un projet", new_place_project_path(@place.id), class: "link-solo" %></p>
    <% end %>
    </div>
  </div>
  <div class="row">
    <div class="col-12 col-lg-8">
    <% @place.projects.each do |project| %>
      <div class="card-project my-5" data-aos="flip-down">
      <%= cl_image_tag project.user.photo.key, crop: :fill %>
        <div class="card-project-infos">
          <h2>Projet de <%= project.user.first_name %> pour <%= @place.title %>
          <% if current_user == project.user %>
          <%= link_to(place_project_path(project), data: {turbo_method: :delete, turbo_confirm: "Etes-vous sûr.e de vouloir supprimer ce projet?"}) do%>
          <i class="fa-solid fa-trash-can"></i>
          <% end %>
          <% end %>
          </h2>
          <h3><%= project.accepted ? "✅ Ce projet est accepté" : "⌚ Ce projet est en attente d'acceptation" %></h3>
          <p><%= project.details %></p>
          <p><%= project.start_date %><% if project.end_date %> - <%= project.end_date%><% end %></p>
        </div>
      </div>
    <% end %>
    </div>
  </div>
</div>
